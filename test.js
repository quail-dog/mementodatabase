/**
Issue a search query to Discogs database.
@param {string} query - Search query.

function Check (query) {
    if( query.includes('INTERAC')) {
    var type = "Interact"
    }
    else {
    var type = "Null"
    }
}
